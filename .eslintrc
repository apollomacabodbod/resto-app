{
    "extends": [
     "react-app",
     "react-app/jest"
   ],
     "rules": {
       "no-console": "warn", // or "error" if you want it to be treated as an error
       "indent": ["warn", 2] // Enforce 2-space indent
     }
   }